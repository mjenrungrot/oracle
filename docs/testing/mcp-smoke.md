# MCP smoke tests

MCP smoke tests are deprecated in this local fork because MCP is no longer a supported runtime surface.

Current validation should focus on browser-mode CLI flows instead:

- `pnpm run build`
- targeted browser CLI previews / dry-run prefills
- browser session/status/restart checks
