# MCP status

MCP is deprecated in this local fork.

- `oracle-mcp` no longer starts an MCP server
- `oracle oracle-mcp` is also deprecated
- `oracle bridge codex-config` and `oracle bridge claude-config` now fail with guidance instead of printing MCP snippets

Use `oracle` directly for local ChatGPT browser runs, or `oracle serve` when you need remote browser automation.
